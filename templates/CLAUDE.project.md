# PROJECT — SuperStack Enabled
# Inherits the global Master Router from ~/.claude/CLAUDE.md.
# Add project-specific rules below. Do not remove the memory block.

## Memory
- Vault: `.vault/` (hot.md = recent context, index.md = topics, wiki/ = pages)
- Code graph: `graphify-out/`
- ALWAYS check vault + graph before asking the user for context.

## Project-Specific Guidelines
- (add your stack, conventions, and constraints here)
